{
  "name": "registryvault",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @registryvault/web dev",
    "build": "pnpm --filter @registryvault/web build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
